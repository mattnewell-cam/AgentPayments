PORT=8787
APP_BASE_URL=http://localhost:8787
MASTER_KEY=yH9vB4sQ!2Gz#nP7uX@5mK3LwR8cT1JfD6
SOLANA_RPC_URL=https://api.devnet.solana.com
PAYMENTS_DRY_RUN=false
# Optional: persistent storage (recommended for Vercel). If unset, falls back to local JSON file.
# DATABASE_URL=postgres://user:password@host/dbname?sslmode=require
# Optional fallback wallet when devnet faucet is rate-limited:
# BOT_WALLET_SECRET_KEY=<base58 secret key>
# or set BOT_WALLET_FILE (default: ../jsons/bot-wallet.json)
BOT_WALLET_FILE=../jsons/bot-wallet.json
